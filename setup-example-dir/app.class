# Class named "app" for bash appect

# field
declare -Ag app_fields

app.field(){
    if [ "$2" == "=" ]
    then
        app_fields[$1]=$3
    else
        echo ${app_fields[$1]}
    fi
}

app.fileName(){
    app.field fileName $1 $2
}


